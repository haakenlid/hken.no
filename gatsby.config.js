/* eslint-disable no-param-reassign */
module.exports = (config, env) => {
  // config.loader('meta', cfg => {
  //   cfg.test = /\.meta$/
  //   cfg.loader = 'null'
  //   return cfg
  // })
  return config
}
